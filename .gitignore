# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
.pytest_cache/

# Generic cache (npm, pip, etc.)
.cache/

# PyInstaller 중간/최종 산출물 (build 디렉토리 자체는 스크립트/spec 포함이라 유지)
build/InfiniteClipboard/
build/infinite-clipboard/
build/Output/
build/*.log
# Arch makepkg 작업/산출물
build/pkg/
build/src/
build/*.pkg.tar.zst
# assets/generated/ 는 git에 포함 — 각 PC가 rsvg-convert 없이 바로 빌드 가능하도록

# OS
.DS_Store
Thumbs.db

# IDE / 편집기 임시
.idea/
.vscode/
*.swp
*.bak

# App runtime
*.log
settings.json

# Claude Code
.claude/
.next-tasks.json
.next-tasks.archive.jsonl
# 채팅에 붙여넣은 이미지 임시 업로드 폴더 — 개인 스크린샷(계정 정보 등) 포함 가능,
# 실수로 커밋되는 걸 막기 위해 반드시 ignore (2026-07-10 git add -A 로 스테이징될 뻔함)
.uploads/
# 개발 방법론/함정 노트 — 로컬 전용 자산, public repo엔 미공개(2026-07-10)
/CLAUDE.md
/phase-gates.md
# brainstorming/writing-plans 산출 설계 문서 — 과거 docs/ 전체 삭제(2026-07-10)와
# 같은 이유로 로컬 전용 유지, public repo에는 미공개
/docs/plans/

# Serena agent local state
.serena/

# v3.0 lazy-clipboard 스파이크 — pywayland-scanner 생성 바인딩 (CI/로컬이 재생성)
spikes/lazy-clipboard/_proto/

# 코드 백업 폴더 (git history 가 그 역할)
BK/

# 루트 빌드 산출물 패키지 (각 OS installer/dmg/pkg)
*.dmg
*.exe
*.msi
*.AppImage
*.deb
*.rpm
*.tar.gz
*.tar.xz
